@model NewsApp.ViewModels.NewsViewModel

<h1>Новости в категории @Model.Articles.First().Category.Name</h1>
<br />

@foreach (Article article in Model.Articles)
{
    <div>
        <div><h3><a asp-action="ShowArticle" asp-route-id="@article.Id">@article.Title</a></h3></div>
        <div>@article.Author</div>
        <div>@article.Date</div>
        <div>@article.Category.Name</div>
        <p>@article.Preview</p>
    </div>
}

<nav>
    <ul class="pagination">
        @if (Model.PageViewModel.HasPreviousPage)
        {
            <li class="page-item">
                <a asp-action="NewsByCategory"
                   asp-route-page="@(Model.PageViewModel.PageNumber - 1)"
                   asp-route-categoryId="@Model.Articles.First().Category.Id"
                   class="page-link">Предыдущая</a>
            </li>

        }
        @if (Model.PageViewModel.HasNextPage)
        {
            <li class="page-item">
                <a asp-action="NewsByCategory"
                   asp-route-page="@(Model.PageViewModel.PageNumber + 1)"
                   asp-route-categoryId="@Model.Articles.First().Category.Id"
                   class="page-link">Следующая</a>
            </li>
        }
    </ul>
</nav>
